/**
 * Represents a Grid.
 * @constructor
 * @param {integer} x coordinate
 * @param {integer} y coordinate
 */
class Grid {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}

module.exports = Grid;